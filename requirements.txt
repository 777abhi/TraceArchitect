# Day 1 dependencies for the Agentic AI & GenAI Test Orchestration learning path.
# LLM backend: local Ollama (fully offline, no hosted/paid API key).
# Requires Python 3.11+.
#
# Install with:  pip install -r requirements.txt
# Then:          playwright install

playwright==1.55.0
python-dotenv==1.1.1
ollama==0.5.4

# Day 2 — structured outputs / JSON mode.
pydantic==2.11.7
